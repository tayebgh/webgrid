import PricingClient from "./PricingClient";

export const metadata = {
  title: "Pricing — WebGrid",
  description: "WebGrid Free vs Pro. Upgrade to Pro for unlimited bookmarks, no ads, and priority support.",
};

export default function PricingPage() {
  return <PricingClient />;
}
